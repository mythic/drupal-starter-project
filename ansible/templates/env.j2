# {{ ansible_managed }}
#
# The variables in this file are controlled via the
# ansible/vars/environment.yml file. Sensitive values should be encrypted using
# ansible-vault.
#
# To add custom environment variables, create a .env.local file.
{% for key, value in env_vars.items() -%}
  {{ prefix | default('') }}{{ key }}="{{ value }}"
{% endfor %}
